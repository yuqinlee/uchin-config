" ====================================
" add by uchin
" ====================================
set number 		"显示行号
syntax on 		"打开语法高亮
set showmode 		"在底部显示，当前处于命令模式还是插入模式
set showcmd 		"命令模式下底部显示键入的指令,如键入2y3d，那么底部就会显示2y3，当键入d的时候，操作完成，显示消失
set mouse=a 		"支持使用鼠标
set encoding=utf-8  	"使用 utf-8 编码
set t_Co=256 		"启用256色

